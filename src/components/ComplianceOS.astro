<style>
/* ── COMPLIANCE OS ── */
.cos-sec { background:var(--green);padding:110px 60px;text-align:center;position:relative;overflow:hidden; }
.cos-sec::before { content:'';position:absolute;inset:0;background:radial-gradient(circle at 50% 50%,rgba(201,168,76,.1) 0%,transparent 60%);pointer-events:none; }
.cos-eyebrow { font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.18em;color:rgba(255,255,255,.3);margin-bottom:20px; }
.cos-headline { font-family:'Playfair Display',serif;font-size:50px;font-weight:800;color:#fff;line-height:1.15;margin-bottom:20px; }
.cos-headline em { color:var(--gold);font-style:normal; }
.cos-desc { font-size:16px;color:rgba(255,255,255,.6);line-height:1.8;max-width:540px;margin:0 auto 52px; }
.cos-connects { display:flex;justify-content:center;align-items:center;gap:0;flex-wrap:wrap;position:relative;z-index:1; }
.cos-node { background:rgba(255,255,255,.07);border:1px solid rgba(255,255,255,.14);border-radius:10px;padding:12px 20px;font-size:12px;font-weight:700;color:rgba(255,255,255,.8);white-space:nowrap; }
.cos-arrow { font-size:20px;color:var(--gold);padding:0 6px;opacity:.5; }

.btn-gold { background:linear-gradient(135deg,var(--gold),var(--gold2));color:var(--green);padding:14px 30px;border-radius:10px;font-size:14px;font-weight:800;text-decoration:none;display:inline-flex;align-items:center;gap:8px;box-shadow:0 4px 24px rgba(201,168,76,.35);transition:transform .2s,box-shadow .2s; }
.btn-gold:hover { transform:translateY(-2px);box-shadow:0 8px 32px rgba(201,168,76,.45); }

@media(max-width:900px){
  .cos-sec { padding:60px 24px; }
  .cos-headline { font-size:32px; }
}
</style>

<section class="cos-sec">
  <div style="position:relative;z-index:1;max-width:1100px;margin:0 auto">
    <div class="cos-eyebrow">Notre positionnement</div>
    <div class="cos-headline">Gëstu n'est pas un outil de plus.<br/>C'est le <em>système d'exploitation</em><br/>de votre conformité.</div>
    <p class="cos-desc">Une plateforme qui connecte normes, processus internes, contrôles, données et équipes — pour piloter en temps réel l'ensemble de vos obligations.</p>
    <div class="cos-connects">
      <div class="cos-node">Normes<br/><span style="font-weight:400;font-size:10px;opacity:.6">ISO · NIS2 · RGPD · COBIT</span></div>
      <div class="cos-arrow">→</div>
      <div class="cos-node">Processus<br/><span style="font-weight:400;font-size:10px;opacity:.6">internes</span></div>
      <div class="cos-arrow">→</div>
      <div class="cos-node" style="background:rgba(201,168,76,.15);border-color:rgba(201,168,76,.3)">Gëstu<br/><span style="font-weight:400;font-size:10px;color:var(--gold)">plateforme centrale</span></div>
      <div class="cos-arrow">→</div>
      <div class="cos-node">Contrôles<br/><span style="font-weight:400;font-size:10px;opacity:.6">&amp; preuves</span></div>
      <div class="cos-arrow">→</div>
      <div class="cos-node">Décisions<br/><span style="font-weight:400;font-size:10px;opacity:.6">stratégiques</span></div>
    </div>
    <div style="margin-top:48px;display:flex;justify-content:center">
      <a href="#contact" class="btn-gold" style="font-size:14px">Voir comment ça fonctionne →</a>
    </div>
  </div>
</section>
